# Pinned to the versions this repo's numbers were measured on (env: vipe-new,
# Python 3.11.15). torch/torchvision need the matching CUDA index -- see README
# "Install" -- so install them first, then the rest of this file.
ray==2.55.1
lpips==0.1.4
pytorch-fid==0.3.0
scikit-learn==1.9.0
transformers==4.57.6
opencv-python==4.13.0.92
hydra-core==1.3.3
omegaconf==2.3.1
scipy==1.17.1
tqdm==4.68.3
nvidia-ml-py==13.610.43

# data handling
numpy==2.4.6
pandas==3.0.5
